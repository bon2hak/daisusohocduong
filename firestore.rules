rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public read & write for educational portal state synchronization
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
